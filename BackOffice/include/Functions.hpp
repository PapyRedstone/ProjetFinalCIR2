#ifndef FUNCTIONS_HPP
#define FUNCTIONS_HPP

#include <vector>
#include <map>
#include <algorithm>
#include <string>

std::string toLower(const std::string&);
std::vector<std::string> getWords(std::string, char separator);

#endif
