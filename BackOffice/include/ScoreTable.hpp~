#ifndef TABLE_HPP
#define TABLE_HPP

#include <QtWidgets>
#include <memory>
#include "Database.hpp"

class ScoreTable{
public:
  ScoreTable(std::shared_ptr<Database> db, QTableWidget* t):table{t}, database{db}{}
  ~ScoreTable(){}

  void loadTable();

private:
  QTableWidget* table;
  std::shared_ptr<Database> database;
};

#endif
