#ifndef QUESTION_H
#define QUESTION_H

#include <QDialog>
#include "mainwindow.h"
#include "Database.hpp"

namespace Ui {
class Question;
}

class Question : public QDialog
{
    Q_OBJECT

public:
    explicit Question(Database& db, QWidget *parent = 0);
    ~Question();

private slots:
  void on_Utilisateur_clicked();

private:
    Ui::Question *ui;
    Database& database;
};

#endif // QUESTION_H
